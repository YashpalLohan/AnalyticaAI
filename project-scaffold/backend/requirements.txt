# =============================================================
# AnalyticaAI — Backend Dependencies
# Pin exact versions for reproducibility.
# =============================================================

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Configuration
pydantic==2.8.2
pydantic-settings==2.4.0

# Database
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# Background Jobs
celery==5.4.0
redis==5.1.0

# AI / LLM
langchain==0.3.3
langchain-groq==0.2.3          # Groq LLM integration for LangChain
langchain-community==0.3.3
langgraph==0.2.28
groq==0.11.0                   # Groq Python SDK

# Embeddings (Groq does not support embeddings — using Nomic)
langchain-nomic==0.1.3         # Nomic embeddings via API or Ollama
langchain-ollama==0.2.1        # Ollama local embeddings (nomic-embed-text)

# Vector Database
chromadb==0.5.11

# Data Processing
pandas==2.2.3
numpy==1.26.4
openpyxl==3.1.5

# Data Profiling
ydata-profiling==4.10.0

# Visualization
plotly==5.24.1
matplotlib==3.9.2
seaborn==0.13.2

# Machine Learning
scikit-learn==1.5.2
xgboost==2.1.1
lightgbm==4.5.0

# Forecasting
prophet==1.1.5

# Report Generation
reportlab==4.2.2
python-docx==1.1.2

# Storage — Supabase (free tier: 1GB)
supabase==2.9.1                # Supabase Python client (storage + DB)

# HTTP Client
httpx==0.27.2

# Utilities
python-dotenv==1.0.1
structlog==24.4.0
sentry-sdk[fastapi]==2.15.0

# Testing (dev only)
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2
factory-boy==3.3.1
